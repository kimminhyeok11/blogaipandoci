{
  "scripts": {
    "build:minify": "node scripts/build-minify.js",
    "build:bundle": "node scripts/build-bundle.js",
    "build": "npm run build:minify && npm run build:bundle"
  },
  "dependencies": {
    "chrome-launcher": "^1.2.1",
    "isomorphic-git": "^1.34.2",
    "lighthouse": "^13.0.1",
    "esbuild": "^0.23.0"
  }
}
